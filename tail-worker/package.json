{
	"name": "vibesdk-production-tail",
	"version": "1.0.0",
	"private": true,
	"description": "Tail worker for VibeSDK production log monitoring",
	"main": "index.ts",
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"tail": "wrangler tail vibesdk-production-tail"
	}
}
